#!/usr/bin/env bash

# load machine-local config if present
source_env_if_exists .envrc.local

watch_file mise.toml
if ! mise install >&2; then
  log_error "mise install failed - tools from mise.toml are missing from PATH"
fi
